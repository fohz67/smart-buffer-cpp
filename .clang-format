---
# Fichier de configuration clang-format pour projets C++
# Ce fichier uniformise le formatage de votre code et peut être placé à la racine de chacun de vos projets.
# Basé sur le style Google avec quelques ajustements personnels.

# Définition du langage
Language:        Cpp
BasedOnStyle:    Google

# Indentation et tabulations
IndentWidth:     4         # Utilise 4 espaces pour l'indentation.
TabWidth:        4         # Largeur d'une tabulation.
UseTab:          Never     # Utilise toujours des espaces (évite les tabulations).

# Limite de colonnes
ColumnLimit:     100       # Limite le nombre de caractères par ligne à 100.

# Formatage des accolades
BreakBeforeBraces: Allman   # Place l'accolade ouvrante sur une nouvelle ligne.

# Alignement des pointeurs et références
DerivePointerAlignment: false
PointerAlignment: Left      # Aligne les pointeurs à gauche (ex. int* ptr).

# Gestion des espaces et parenthèses
SpaceBeforeParens: ControlStatements  # Ajoute un espace avant la parenthèse dans les structures de contrôle (if, for, while, etc.).
SpacesInParentheses: false             # Aucun espace à l'intérieur des parenthèses.
SpacesInAngles:    false               # Aucun espace à l'intérieur des chevrons (< >).

# Paramètres de fonctions et appels
AllowAllParametersOfDeclarationOnNextLine: false
BinPackParameters: false   # N'essaie pas de regrouper plusieurs paramètres sur une seule ligne.
BinPackArguments: false    # N'essaie pas de regrouper plusieurs arguments sur une seule ligne.

# Formatage des initialisateurs de constructeur
BreakConstructorInitializers: BeforeColon  # Place la liste d'initialisation sur une nouvelle ligne.
ConstructorInitializerIndentWidth: 4         # Indente la liste d'initialisation avec 4 espaces.

# Autres options de formatage
IndentCaseLabels:  true              # Indente les labels des cas dans les instructions switch.
BreakBeforeTernaryOperators: true     # Force le retour à la ligne avant l'opérateur ternaire.
